# AI 阅闻自有预览图实施计划

1. 为解析器补充红灯测试：`og:image` 与正文图共存时只返回正文图；仅元数据图时返回空图片。
2. 修改 AI HOT 和在花详情提取逻辑，移除页面元数据图片回退；聚合页成功抓取时不混入 RSS/元数据预览卡。
3. 更新同步测试，验证已有全文不被覆盖、正常正文图仍进入 R2/API。
4. 在 AI 阅闻列表为无封面项加入本站 CSS 卡片，仅使用 fly living 自有字段；更新前端契约测试和响应式样式。
5. 运行新闻专项、前端契约、API Worker 全套、类型检查、Lint、秘密扫描和构建验证。
6. 提交并以非强制快进更新功能分支与 `main`；部署 Worker 和 Pages。
7. 生产清空自动来源图片引用，重置来源缓存并等待定时同步；验证已知在花标题卡消失、真实正文图保留。
8. 使用浏览器验证列表和阅读页桌面/移动端，记录 API、R2、控制台与图片加载证据。
