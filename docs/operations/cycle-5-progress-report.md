# 周期 5 进展记录

- 更新：2026-08-05
- 状态：**进行中，首批目标已交付**

## 已进入主线的目标

- 后台快速开始、编辑状态说明、快捷保存、草稿切换保护和移动端底部面板。
- 导航、自述时间线、友链、瞬间、媒体、随心听和 AI 阅闻采用面向内容的表单/卡片，不要求直接编辑 JSON。
- 审核与发布使用创作语言说明状态，合并确认不依赖记忆命令。
- 文章公开查询统一排除草稿；后台与正式页共享 MDC 渲染链；支持多个 macOS 窗口块。
- 文章头部推广由后台管理，首页与文章页共享展示组件。
- `e2e/cycle5.spec.ts` 当前覆盖 12 项桌面/移动工作流。

## 本次推进的首个质量目标

生产链接检查发现后台配置可保存正式域绝对 URL，推广组件会原样输出，导致同源链接被 Nuxt Link Checker 标记为 `absolute-site-urls` 警告。

本轮新增 `normalizeCanonicalSiteHref`：

- `https://flyovo.cc.cd/...` 渲染为相对路径；
- 查询参数和 hash 保留；
- 真实外链保持绝对 URL 与 `noopener sponsored`；
- 浏览器会规范化为外站的反斜杠逃逸不会误判为站内链接。

定向回归：25/25 Pass；最新主线 Nuxt/共享测试 26 files / 222 tests，API Worker 145 tests，Edge Worker 33 tests。

同时为四条 Actions 工作流增加 `CI_RUNNER` 仓库变量回退：变量未设置时仍使用 `ubuntu-latest`；托管 Runner 因账单或额度不可用时，可临时切换到受控的仓库级自托管 Runner，完成后删除变量与 Runner。

## 下一目标

继续以“后台高频任务可理解、可恢复、可验证”为优先级，后续新增范围必须先有独立设计/验收标准，避免把未定义功能混入既有周期。
