name: Quality

on:
  pull_request:
  push:
    branches: ['**']
    paths-ignore:
      - 'backups/**'

permissions:
  contents: read

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ${{ vars.CI_RUNNER || 'ubuntu-latest' }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2
      - name: 文章 PR 快路径策略
        if: ${{ github.event_name == 'pull_request' && startsWith(github.head_ref, 'admin/article/') }}
        run: |
          changed_files="$(git diff --name-only HEAD^1...HEAD^2)"
          test "$(printf '%s\n' "$changed_files" | sed '/^$/d' | wc -l | tr -d ' ')" = 1
          printf '%s\n' "$changed_files" | grep -Eq '^content/posts/[0-9]{4}/[a-z0-9]+(-[a-z0-9]+)*\.md$'
      - uses: pnpm/action-setup@v6
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
        with:
          version: 11.8.0
      - uses: actions/setup-node@v6
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
      - run: pnpm lint
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
      - run: pnpm typecheck
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
      - run: pnpm test:unit
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
      - run: pnpm test:workers
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
      - run: pnpm generate
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
      - run: pnpm check:smoke
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
      - run: pnpm check:links
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
      - run: pnpm check:secrets
        if: ${{ github.event_name != 'pull_request' || !startsWith(github.head_ref, 'admin/article/') }}
